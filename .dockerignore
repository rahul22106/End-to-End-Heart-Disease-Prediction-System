# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.tox
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Git (keep .git excluded but NOT .gitignore)
.git/

# Documentation (but keep README.md for setup.py)
docs/

# Testing
.pytest_cache/
.mypy_cache/
tests/

# Logs (exclude log files but keep logs/ directory structure)
logs/*.log

# Temporary files
temp/
tmp/

# Large data files - but KEEP artifacts/ directory
# The model files in artifacts/ are needed!
data/

# Exclude raw data files if they exist in root
heart.csv
test.csv
train.csv

# Docker files (no need to copy these into the image)
.dockerignore
docker-compose.yml
docker-compose.yaml

# CI/CD files (not needed in container)
.github/

# Deployment config (not needed in container)
render.yaml

# Development files
*.ipynb
.pytest_cache/
htmlcov/

# Environment files
.env
.env.*

# Keep these files/folders:
# - README.md (needed by setup.py)
# - setup.py (needed for -e . installation)
# - requirements.txt (needed for pip install)
# - artifacts/ (needed for trained model)
# - config/ (needed for app configuration)
# - schema.yaml (needed for data validation)
# - param.yaml (needed for model parameters)